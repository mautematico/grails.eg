import com.mx.eglobal.Persona;
class BootStrap {

    def init = { servletContext ->
/*    	def personas = Persona.getAll()

    	personas.each{
    		it.save(failOnError:true);
    		println it;

    	}
*/
    }
    def destroy = {
    }
}
